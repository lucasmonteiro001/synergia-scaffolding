<template name="COLLECTION_NAME">
    {{> pageHeading title='COLLECTION_NAME' }}

    <div class="wrapper wrapper-content white-bg animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">

                <h4 class="page-header">Adicionar COLLECTION_NAME</h4>
                {{> COLLECTION_NAMEAdd}}
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12 col-sm-12">
                <h4 class="page-header">COLLECTION_NAMEs</h4>
                {{> COLLECTION_NAMEView}}
            </div>
        </div>
    </div>

</template>




<template name="COLLECTION_NAMEAdd">
  <div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
      <div class="panel panel-default">
        <div class="panel-heading clearfix">
          <i class="icon-calendar"></i>
          <h3 class="panel-title">Cadastrar COLLECTION_NAME</h3>
        </div>
         <div class="panel-body">
            <form class="form-horizontal row-border" id="userForm" data-toggle="validator" role="form">
                FORM_STRUTURE

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </form>
         </div>
      </div>
    </div>
  </div>

</template>

<template name="COLLECTION_NAMEView">
    <ul class="list-group">
        {{#each COLLECTION_NAMEs}}
            <li class="list-group-item">
                FIELDS_FOR_VIEW_COLLECTIONS
            </li>
        {{else}}
            <p class="alert alert-warning">Nenhum item cadastrado</p>
        {{/each}}
    </ul>
</template>
